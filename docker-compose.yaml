version: "3"
services:
    api:
        build: .
        ports:
            - "8000:8000"
        environment:
            - DATABASE_URL=sqlite:///./finance.db
        volumes:
            - ./finance.db:/app/finance.db